import { css } from '@emotion/react';

const styles = {
    container: css({
        width: "90%",
        height: "100%",
        display: "flex",
        flexDirection: "column",
        justifyContent: "center",
        alignItems: "center"
    }),
    buttons: css({
        width: "100%"
    }),
    button: css({
        width: "50%"
    })
}

export default styles;